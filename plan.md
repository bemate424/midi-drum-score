# MIDI → 드럼 악보 PDF 변환 프로그램 계획

## 프로젝트 개요
- **목표**: SMF(Standard MIDI File)를 업로드하면 드럼 악보 PDF로 출력해주는 프로그램 구축
- **배포 형태**: 로컬 전용 (내 컴퓨터에서만 실행, 배포 없음)
- **UI**: Streamlit (아직 미설치 — 파이프라인 구축 후 설치 예정)
- **작업 폴더**: `C:\Users\HONG\Desktop\score`
- **전체 흐름**: 메인 화면에서 SMF 파일 업로드 → 내부 파이프라인 처리 → 결과 PDF 다운로드/미리보기

## 파이프라인 단계

### 1. 데이터 읽기 및 전처리
- MIDI 파일을 열어 드럼 채널(General MIDI 기준 채널 10 / 노트 넘버 기반 퍼커션 매핑)만 추출
- 드럼과 무관한 메타데이터(트랙명, 가사, 컨트롤 체인지 등 불필요한 정보)는 제거
- 각 노트를 `[시간(초), 노트 번호, 벨로시티]` 형태의 숫자/텍스트 리스트로 변환
  - 예: `[시간: 0.00초, 노트: 36(Kick), 세기: 100]`, `[시간: 0.25초, 노트: 42(Hi-Hat), 세기: 80]`
- 후보 라이브러리: `mido` 또는 `pretty_midi`

### 2. 박자 보정 (Quantization)
- 특정 오차 범위(예: ±20ms) 내에서 밀린 타이밍을 그리드에 맞춰 보정
- 연타/고스트 노트(약한 벨로시티) 판별 로직으로 음악적으로 타당한 박자 형태 결정
  - 고스트 노트 임계 벨로시티 값 설정 필요
- 보정된 노트를 악보용 박자 그리드(예: 16분음표 단위)에 정확히 배치

### 3. 악보 표준 언어 변환 (MusicXML 생성)
- 그리드에 배치된 숫자/박자 정보를 MusicXML 형식으로 변환
  - 예: 노트 36(Kick) + 4분음표 → 오선지 최하단 타원 음표 태그 생성
- 드럼 전용 percussion clef, 악기별 보표 위치(노트 헤드 위치) 매핑 테이블 필요
- 후보 라이브러리: `music21` (MusicXML 생성/조작 지원)

### 4. 시각화 및 문서 출력
- 생성된 MusicXML을 MuseScore(CLI 또는 MCP 연동)로 전달해 PDF/이미지 렌더링
- PDF 다운로드 전 미리보기 및 수정 기능 제공
  - Streamlit 화면에서 렌더링 결과(이미지/PDF)를 확인
  - 필요 시 MusicXML 단계로 돌아가 수동 보정 후 재렌더링 가능하도록 설계

## 참고 사항
- 로컬 전용이므로 인증/보안, 멀티유저 처리 등은 고려하지 않음
- MuseScore 실행 파일 또는 MCP 서버(musescore MCP 도구) 연동 방식 중 택일하여 4단계 구현
- 각 단계는 독립 모듈로 구성하여 중간 산출물(추출 데이터, 보정 데이터, MusicXML)을 단계별로 확인 가능하게 설계
